{
  "name": "bouncing-balls",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm install --omit=optional && vite build",
    "preview": "vite preview",
    "lint": "eslint -c .eslintrc.json --config-type json .",
    "format": "prettier -w ."
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "eslint": "^9.27.0",
    "prettier": "^3.5.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "three": "^0.176.0"
  }
}
